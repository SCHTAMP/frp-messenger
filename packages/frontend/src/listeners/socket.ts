import { io } from "socket.io-client";

const socketUrl = "wss://myhost.local:3000/api/ws";

// const socket = io(socketUrl, {
//   withCredentials: true,
//   transports: ["websocket"],
// });

// export default socket;
